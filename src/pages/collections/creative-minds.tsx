import { CreativeProducts } from "~/components/Products"

const CreativeCollection = () => {
    return (
        <main className="md:wrapper">
            <section className="pt-6">
                <CreativeProducts />
            </section>
        </main>
    )
}

export default CreativeCollection
