import { StaffProducts } from "~/components/Products"

const StaffCollection = () => {
    return (
        <section className="pt-6">
            <StaffProducts />
        </section>
    )
}

export default StaffCollection
